# Python dependencies for F1 Data Engineering Project
# For Databricks Runtime (pre-installed: pyspark, delta-spark, pandas, numpy)

# Note: Most dependencies are pre-installed in Databricks Runtime

# Example additional libraries :
azure-storage-file-datalake>=12.0.0  # For ADLS Gen2 operations outside Databricks
azure-identity>=1.7.0  # For authentication
