# Azure Data Lake Storage Gen2 Mount Configuration
# Replace with your actual storage account details

storage_account_name = "<your-storage-account>"
container_name = "<your-container>"
mount_point = "/mnt/f1data"

# SAS token or service principal credentials
# DO NOT commit actual credentials to the repo
